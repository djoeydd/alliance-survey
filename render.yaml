services:
  - type: web
    name: alliance-survey
    env: node
    buildCommand: npm install && npm run build
    startCommand: node server/src/index.js
    envVars:
      - key: NODE_ENV
        value: production
